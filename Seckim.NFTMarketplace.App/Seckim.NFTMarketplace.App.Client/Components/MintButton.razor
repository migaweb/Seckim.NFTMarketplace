@rendermode InteractiveWebAssembly
@inject NavigationManager NavigationManager

<RadzenButton Icon="gavel"
              Text="Mint"
              ButtonStyle="ButtonStyle.Primary"
              Click=@(args => OnClick())
              class="rz-ripple" />

@code {
  private void OnClick()
  {
    NavigationManager.NavigateTo("/mint");
  }
}